--  Requête permettant de sélectionner le nom et la capacite de la table “salles”:

SELECT nom, capacite  -- SELECT: sélectionner les colonnes (ici "nom" et "capacite"), lire des données
FROM salles;          -- FROM: de la table "salles"

--  Code PHP:   $sql = "SELECT nom, capacite FROM salles;"
